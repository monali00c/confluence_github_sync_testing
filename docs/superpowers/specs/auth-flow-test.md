---
title: Test Auth Flow Spec
sync_to_confluence: true
confluence-labels:
  - technical
  - spec
  - ipms
---

# Test Auth Flow Spec

This is a test spec doc nested under `superpowers/specs/`.

It verifies that:
- Intermediate directory pages (`superpowers`, `specs`) are created automatically
- This page appears as a sub-page with correct nesting
